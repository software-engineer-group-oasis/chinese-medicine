
type ImageUploadProps = {
    single:boolean,
    onSuccess:(url:string)=> void
}

export default function ImageUpload({single, onSuccess}: ImageUploadProps) {
    return (
        <div>

        </div>
    )
}